# Loosened to version ranges so pip resolves wheels for whatever Python
# Streamlit Community Cloud runs (currently 3.13). Exact pins like
# pandas==2.1.4 lack 3.13 wheels and fail to build on deploy.
streamlit>=1.40,<2
pandas>=2.2,<3
plotly>=5.22,<6
